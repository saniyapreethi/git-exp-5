1.Created a repo in git
2.created a folder in vs-code named "exp-5"
3.used coomands to create main 
   git init
   git git config --global user.name "Saniyapreethi" 
   git config --global user.email "Saniyapreethi@gmail.com" 
   git status
   git remote -v
   git remote get-url origin
   git branch
   git remote and origin  https://github.com/saniyapreethi/git-exp-5.git
   git branch -M main
4.Created a README.md file
5.Pushed main
   git add .
   git commit -m "First commit"
   git push  -u origin main
   